"""bc-code-atlas registry: version discovery/resolution and diff/history over
the upstream MSDyn365BC.Sandbox.Code.History repository.

Served as its own MCP-over-HTTP server (registry/registry/mcp_server.py, a
later task), proxied through the aggregator like every other bc-code-atlas
capability (constitution Principle I).
"""
